services:
  - type: web
    name: ubc-api
    env: node
    region: frankfurt
    plan: free
    branch: main
    rootDir: .
    buildCommand: "pnpm install --frozen-lockfile && pnpm --filter=@ubc/api run build && pnpm --filter=@ubc/api exec prisma generate"
    startCommand: "node apps/api/dist/main"
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://ubc-web.vercel.app
      - key: DATABASE_URL
        fromDatabase:
          name: ubc-db
          property: connectionString

databases:
  - name: ubc-db
    plan: free
    region: frankfurt
